import { Routes } from '@angular/router';
import { WorkoutHistoryComponent } from './workout-history.component';

export const WORKOUT_HISTORY_ROUTES: Routes = [
  { path: '', component: WorkoutHistoryComponent },
];
