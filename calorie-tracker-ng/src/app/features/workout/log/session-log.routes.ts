import { Routes } from '@angular/router';
import { SessionLogComponent } from './session-log.component';

export const SESSION_LOG_ROUTES: Routes = [
  { path: '', component: SessionLogComponent },
];
