import { Routes } from '@angular/router';
import { WorkoutAnalyticsComponent } from './workout-analytics.component';

export const WORKOUT_ANALYTICS_ROUTES: Routes = [
  { path: '', component: WorkoutAnalyticsComponent },
];
