import { Routes } from '@angular/router';
import { WorkoutStreaksComponent } from './workout-streaks.component';

export const WORKOUT_STREAKS_ROUTES: Routes = [
  { path: '', component: WorkoutStreaksComponent },
];
